{
  "name": "rice-delivery-app",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "concurrently": "^6.0.2",
    "server": "^1.0.4"
  },
  "scripts": {
    "start": "concurrently -n server,app -c green,cyan \"node server.js\" \"cd client && npm run start\"",
    "build": "cd client && npm run build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
